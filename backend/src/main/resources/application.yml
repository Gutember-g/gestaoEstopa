spring:
  application:
    name: erp-multitenant-backend
  profiles:
    active: dev

server:
  port: 8080
  servlet:
    context-path: /api

jwt:
  header: Authorization
  prefix: "Bearer "
  issuer: gestao-estopa-api
  audience: gestao-estopa-web

